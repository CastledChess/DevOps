locals {
  # database_credentials = jsondecode(base64decode(data.scaleway_secret_version.database_credentials.data))
  # vaultwarden_config = jsondecode(base64decode(data.scaleway_secret_version.vaultwarden_config.data))
}
